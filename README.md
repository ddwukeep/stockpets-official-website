# StockPets

A cozy, persistent virtual pet for market people. Built with React, Vite, SVG, and CSS.

## Development

```bash
npm install
npm run dev
```

Progress is stored locally in the browser. The production build uses the GitHub Pages base path `/stockpets-official-website/`.
